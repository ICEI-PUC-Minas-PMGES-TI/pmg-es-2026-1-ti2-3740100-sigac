# URL pública do backend (Render), sem barra no final.
# Configure na Vercel em: Settings → Environment Variables
# API_URL=https://sigac-api.onrender.com
#
# Em desenvolvimento local, deixe vazio: o Next.js faz proxy /api-back → localhost:8080
